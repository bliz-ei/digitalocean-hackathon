import React from "react";import {createRoot,Root} from "react-dom/client";import type {Claim} from "@verity/contracts";import {VerdictCard} from "@verity/ui";const id="verity-shadow-host";let root:Root|undefined;
function mount(claim?:Claim){let host=document.getElementById(id);if(!host){host=document.createElement("aside");host.id=id;document.body.append(host)}const shadow=host.shadowRoot??host.attachShadow({mode:"open"});let target=shadow.querySelector("#verity-root") as HTMLElement|null;if(!target){target=document.createElement("div");target.id="verity-root";shadow.append(target)}root??=createRoot(target);root.render(claim?<VerdictCard claim={claim}/>:<section role="status">Verity ready — start fixture from the extension.</section>)}
mount();void chrome.storage.local.get("claim").then(({claim})=>claim&&mount(claim));chrome.runtime.onMessage.addListener(message=>{if(message.type==="CLAIM")mount(message.claim);if(message.type==="STOP"){root?.unmount();root=undefined;document.getElementById(id)?.remove()}});
